'use strict';

describe('Service: dgrid/dgrid3', function () {

  // load the service's module
  beforeEach(module('angularSpreadsheetApp'));

  // instantiate service
  var dgrid/dgrid3;
  beforeEach(inject(function (_dgrid/dgrid3_) {
    dgrid/dgrid3 = _dgrid/dgrid3_;
  }));

  it('should do something', function () {
    expect(!!dgrid/dgrid3).toBe(true);
  });

});
