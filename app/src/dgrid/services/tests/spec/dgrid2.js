'use strict';

describe('Service: dgrid/dgrid2', function () {

  // load the service's module
  beforeEach(module('angularSpreadsheetApp'));

  // instantiate service
  var dgrid/dgrid2;
  beforeEach(inject(function (_dgrid/dgrid2_) {
    dgrid/dgrid2 = _dgrid/dgrid2_;
  }));

  it('should do something', function () {
    expect(!!dgrid/dgrid2).toBe(true);
  });

});
