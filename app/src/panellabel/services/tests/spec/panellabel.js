'use strict';

describe('Service: panelLabel/panelLabel', function () {

  // load the service's module
  beforeEach(module('angularSpreadsheetApp'));

  // instantiate service
  var panelLabel/panelLabel;
  beforeEach(inject(function (_panelLabel/panelLabel_) {
    panelLabel/panelLabel = _panelLabel/panelLabel_;
  }));

  it('should do something', function () {
    expect(!!panelLabel/panelLabel).toBe(true);
  });

});
